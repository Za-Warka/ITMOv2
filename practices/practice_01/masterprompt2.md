Role: ai senior reviewer & SDLC drafter
note: Модель ничего не знает о процессе занятий; работай только по входам и правилам ниже.

Inputs: @practices/practice_01/TRAINING_PR.diff and:
    - SEC-1: перед отправкой во внешний LLM из diff удаляются токены, пароли и приватные ключи.
    - API-1: diff длиннее 20 000 символов отклоняется с HTTP 413.
    - REL-1: внешний LLM-вызов имеет timeout 10 секунд; ошибка превращается в контролируемый ответ.
    - OUT-1: ответ содержит summary, массив risks и массив checks. В risks максимум три элемента с полями file, line, evidence, risk.
    - SCOPE-1: сервис только советует; он не пишет код и не выполняет действия в GitHub.
    - QA-1: риск включается в ответ, только если он подтверждён строкой diff или правилом репозитория.
    - OBS-1: в лог пишутся только request_id, длительность и статус; содержимое diff и ответа модели не логируется.

context: AI‑помощник code review. На вход — TRAINING_PR.diff. На выход — структурированный результат для человека (OUT‑1). Решение всегда принимает человек (SCOPE‑1).

language: Русский.
forbidden: approve, merge, edit; добавлять собственные правила; логировать содержимое diff/ответа.
DONT MAKE YOUR OWN RULES

goal: Подготовить артефакты Практики 1 для первого инкремента и записать строку P1‑03 в prompts.md.
scope_out: Не изменять код, не деплоить, не выполнять внешние действия.

deliverables:
  - context.md: заполнить таблицу “Продукт и команда сейчас”; раздел “## Context Pack” (факты/правила; формат входа/результата; ограничения/запреты; хороший/плохой пример; неизвестно); “## Как использовали AI”.
  - analysis.md: AS IS; TO BE (```mermaid); таблица “Разница”; “## Как использовали AI”.
  - product_management.md: первый сценарий; Use case; user stories + acceptance criteria (```gherkin); “## Как использовали AI”.
  - project_management.md: таблица инкрементов (наблюдаемый результат, роли человека/AI, проверки, зависимости); диаграмма Ганта (```mermaid) с датами‑заглушками; “## Как использовали AI”.
  - adr.md: статус/дата/ответственные; контекст; решение; альтернативы; последствия и главный риск + как проверяется; архитектурная схема (```mermaid); “## Как использовали AI”.
  - tests_unit.md: таблица изолированных проверок (SEC‑1 редактирование; OUT‑1 формат; валидация входа); “## Как использовали AI”.
  - tests_integration.md: API‑1 (413 > 20k без вызова LLM); REL‑1 (таймаут 10s → контролируемый ответ); OUT‑1 end‑to‑end; “## Как использовали AI”.
  - tests_load.md: краткий сценарий с пределами или обоснование отсрочки + условие включения; “## Как использовали AI”.
  - tests_e2e.md: позитивный/негативный/граничный сценарии с наблюдаемым результатом; “## Как использовали AI”.
  - prompts.md: одна строка P1‑03 с ссылкой на этот мастер‑промпт, кратким результатом, что приняли/отклонили, как проверили.

flow: candidate -> evidence -> check
  - Найди кандидатов на риски/проверки в TRAINING_PR.diff и правилах.
  - evidence: приведи file:line + цитату строки diff или прямую ссылку на правило.
  - check: сформулируй воспроизводимую проверку.
  - Если нет evidence — пропусти.

quality_gates:
  - Соответствие Makefile: 
    context.md содержит “## Context Pack”; problem.md содержит “## Метрики”; analysis.md содержит “## TO BE”; product_management.md содержит блок ```gherkin; project_management.md содержит блок ```mermaid; adr.md содержит “## Архитектурная схема”; prompts.md содержит P1‑01, P1‑02, P1‑03.
  - Не более 3 risks (OUT‑1).
  - Не придумывать детали вне diff/правил; неизвестное помечать “Требуется решение команды: …”.

return:
  - Внеси изменения напрямую в файлы под каталогом @practices/practice_01, без вывода содержимого артефактов в чат:
    - @practices/practice_01/context.md — полный файл (таблица; “## Context Pack”; “## Как использовали AI” с ID P1‑03).
    - @practices/practice_01/analysis.md — AS IS; TO BE (```mermaid); “Разница”; “## Как использовали AI”.
    - @practices/practice_01/product_management.md — сценарий; Use case; ```gherkin; “## Как использовали AI”.
    - @practices/practice_01/project_management.md — инкременты; ```mermaid Gantt; “## Как использовали AI”.
    - @practices/practice_01/adr.md — заполненные разделы + ```mermaid; “## Как использовали AI”.
    - @practices/practice_01/tests_unit.md — таблица; “## Как использовали AI”.
    - @practices/practice_01/tests_integration.md — таблица; “## Как использовали AI”.
    - @practices/practice_01/tests_load.md — сценарий/обоснование; “## Как использовали AI”.
    - @practices/practice_01/tests_e2e.md — таблица; “## Как использовали AI”.
    - @practices/practice_01/prompts.md — добавь одну строку таблицы с ID P1‑03.
  - Перед записью проверь структуру репозитория и используй именно каталог @practices/practice_01 (если есть одноимённая папка вне practices/, её игнорируй).
  - В чат допускается вернуть только краткий статус выполнения (успех или причина stop_if), без включения содержимого артефактов.

stop_if:
  - TRAINING_PR.diff пуст или повреждён.
  - Нет ни одного подтверждённого риска по QA‑1.

checklist:
  - Все требуемые заголовки/блоки присутствуют (см. quality_gates).
  - ≤3 risks; каждый с evidence и check.
  - Не добавлены собственные правила.

result: Готовые тексты записаны в указанные файлы и одна строка P1‑03 добавлена в таблицу из prompts.md.
done: Артефакты согласованы с SEC‑1, API‑1, REL‑1, OUT‑1, SCOPE‑1, QA‑1, OBS‑1; неизвестное отмечено “Требуется решение команды: …”.