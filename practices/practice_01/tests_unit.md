# Unit-проверки

| Требование или правило | Что проверяем изолированно | Вход | Ожидаемый результат | Evidence |
|---|---|---|---|---|
| SEC-1: удалить токены/пароли/приватные ключи до LLM | Функция редактирования секрета перед формированием prompt | diff с `token=abc123`, `password=secret`, кусок `-----BEGIN PRIVATE KEY-----\nMIIC...` | В prompt нет `abc123`, `secret` и блока приватного ключа | app/review_service.py:19-22 — `prompt = f"Review this pull request...\n{diff}"` и `answer = self.llm.generate(prompt)` (сырой diff уходит в LLM) |
| OUT-1: структура ответа | Формирование ответа сервиса | валидный небольшой diff | Возвращается объект с `summary`, `risks[]≤3` (c `file`,`line`,`evidence`,`risk`), `checks[]` | app/review_service.py:22 — `return {"comment": answer}` (несоответствие OUT-1) |
| Валидация входа | Проверка обязательности и типа поля `diff` | payload без `diff` или `diff` не строка | Валидационная ошибка на уровне API | app/api.py:35-37 — доступ по `payload["diff"]` без модели и проверки |

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-03.
- Что проверили и исправили сами: связали тесты с правилами и строками diff; исключили не подтверждённые гипотезы; оформили проверяемые ожидаемые результаты.
