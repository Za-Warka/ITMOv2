# Нагрузочные проверки

| Сценарий | Нагрузка и длительность | Допустимый предел | Что измеряем | Evidence |
|---|---|---|---|---|
| Высокая частота POST /api/reviews с небольшими diff | 50 RPS, 5 минут | Ошибок 5xx = 0; медиана латентности < 200 мс | Латентность, ошибки, соблюдение REL-1 | Требуется решение команды: инструменты и стенд |
| API-1: вход > 20000 символов → 413 | 5 RPS, 3 минуты | 100% HTTP 413; 5xx = 0; вызов LLM не производится | Доля 413; отсутствие вызовов внешнего LLM | Ссылка на правило API-1 из CONTEXT.md; логи без содержимого (OBS-1) |

Запускается после стабилизации API и готовности стенда с телеметрией: включены SEC-1, API-1, REL-1; триггер включения — готовность этих инвариантов.

## Как использовали AI

- Строка в [`prompts.md`](prompts.md): P1-03.
- Без скрытой цепочки мыслей: CoT не используется.
- Что проверили и исправили сами: решили отложить нагрузочные тесты до появления стабильной реализации; условие включения — готовность API c обработкой SEC-1/API-1/REL-1 и стенд с телеметрией.
