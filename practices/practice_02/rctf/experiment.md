# R.C.T.F. Эксперимент

Role
- Инженер-ассистент в репозитории; пишет по-русски (ASCII); использует Read/Glob/Grep/apply_patch; без внешней сети.

Context
- Исходный артефакт: `practices/practice_01/tests_load.md` — краткая таблица нагрузочных проверок.
- Контекст и правила: `practices/practice_01/CONTEXT.md` (SEC-1, API-1, REL-1, OUT-1, SCOPE-1, QA-1, OBS-1).
- Цель: сделать документ по нагрузочным проверкам понятнее и шире по охвату; добавить план, метрики, пороги, Evidence и требования к среде/инструментам.

Task
1) Прочитать файлы `practices/practice_01/tests_load.md` и при необходимости `practices/practice_01/CONTEXT.md`.
2) Подготовить улучшенную версию документа "Нагрузочные проверки" с введением, расширенными сценариями (baseline, ramp, spike, stress, soak, негативные для API-1/REL-1/SEC-1/OUT-1), метриками и порогами, Evidence, требованиями к среде/инструментам, порядком запуска и условиями отсрочки.
3) Записать результат в `practices/practice_02/rctf/tests_load.md` и оформить этот файл-отчёт эксперимента.

Format
- Markdown, русский язык (ASCII). Менять можно только два файла в `practices/practice_02/rctf/`.

Полный запрос
- Role:

Что получили
- Новый документ `practices/practice_02/rctf/tests_load.md` с:
  - кратким вводом и целями;
  - расширенной таблицей сценариев (baseline, ramp, spike, stress, soak, API-1, REL-1, SEC-1/OBS-1, OUT-1/QA-1);
  - разделами: "Метрики и пороги (pass/fail)", "Среда, инструменты и безопасность", "Порядок запуска и воспроизводимость", "Когда нагрузочные тесты откладываются", "Как использовали AI";
  - явными порогами, критериями pass/fail, перечнем Evidence и защитой от утечек (SEC-1/OBS-1).

Что изменили в исходном артефакте
- Файл и раздел: `practices/practice_01/tests_load.md` — содержимое переосмыслено и расширено в новом документе (исходный не изменялся).
- Изменение: добавлены новые сценарии (ramp/spike/stress/soak и негативные), уточнены метрики (p50/p90/p99, RPS, 4xx/5xx, timeouts, 413, CPU/Memory), введены пороги, добавлены требования к среде и инструментарий (k6), определены Evidence и порядок запуска.

Как проверили
- Сопоставили сценарии и пороги с правилами SEC-1, API-1, REL-1, OUT-1, QA-1, OBS-1.
- Убедились, что нет примеров с реальными секретами и содержимым diff/ответов.
- Проверили, что документ самодостаточен, содержит команды запуска и критерии pass/fail для каждого сценария.

Что отклонили
- Нагрузочные проверки прод-среды и использование реальных данных — отклонено из соображений безопасности (SEC-1/OBS-1).
- Добавление альтернативных инструментов (JMeter/Gatling) в качестве основных — оставлены как допустимые, но выбран k6 для конкретики и простоты.
- Автонастройка ретраев LLM и агрессивные ретраи под нагрузкой — не включены, так как политика ретраев не определена ("Требуется решение команды").
